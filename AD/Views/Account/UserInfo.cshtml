@model AD.Controllers.AccountController.AccountModel

@{
    ViewData["Title"] = "UserInfo";
    // Layout = "~/Views/Shared/_Layout.cshtml";
}

<div class="row">
    <form method="post" action="@Url.Action("UserInfo", "Account")">
        <div asp-validation-summary="ModelOnly" class="text-danger"></div>
        <input type="hidden" asp-for="UserAccountName.id" />
        <div class="form-group">
            <label asp-for="UserAccountName.UserName" class="control-label">Имя пользователя</label>
            <input asp-for="UserAccountName.UserName" class="form-control" readonly />
            @* <span asp-validation-for="UserAccountName.UserName" class="text-danger"></span> *@
        </div>
        <div class="form-group">
            <label asp-for="UserAccountName.FirstName" class="control-label"></label>
            <input asp-for="UserAccountName.FirstName" class="form-control" readonly />
            @* <span asp-validation-for="UserAccountName.FirstName" class="text-danger"></span> *@
        </div>
        <div class="form-group">
            <label asp-for="UserAccountName.LastName" class="control-label"></label>
            <input asp-for="UserAccountName.LastName" class="form-control" readonly />
            @* <span asp-validation-for="UserAccountName.LastName" class="text-danger"></span> *@
        </div>
        <div class="form-group">
            <label asp-for="UserAccountName.SurName" class="control-label"></label>
            <input asp-for="UserAccountName.SurName" class="form-control" readonly />
            @* <span asp-validation-for="UserAccountName.SurName" class="text-danger"></span> *@
        </div>
        <p></p>
        <div class="modal-footer">
            @* <input type="submit" value="Добавить" class="btn btn-primary" /> *@
            @* <input type="cancel" value="Закрыть" class="btn btn-primary"/> *@
            <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Закрыть</button>
        </div>

        @* <div class="modal-footer">
        <button type="submit" class="btn btn-primary">Добавить</button>
        @* <button type="submit" class="btn btn-primary">Добавить</button>
        <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Закрыть</button>
        @* <button type="button" class="btn btn-primary">Добавить</button>
        </div> *@

    </form>

</div>
@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
}
