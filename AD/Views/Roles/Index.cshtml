@model AD.Controllers.RolesController.RolesModel

<h2>Список ролей</h2>
<a asp-action="Create">Добавить роль</a>
<table class="table">
    @foreach (var role in Model.Roles)
    {
        <tr>
            <td>@role.Name</td>
            <td>
                <form asp-action="Delete" asp-route-id="@role.Id" method="post">
                    <button type="submit" class="btn btn-sm btn-danger">
                        Удалить
                    </button>
                </form>
            </td>
        </tr>
    }
</table>
<h2>Список Пользователей</h2>
<table class="table">
    @foreach (var user in Model.Users)
    {
        <tr>
            <td>@user.UserName</td>
            <td>
                <a class="btn btn-sm btn-primary" asp-action="Edit" asp-route-userid="@user.Id">Права доступа</a>
                @* <form asp-action="Delete" asp-route-id="@user.Id" method="post">
                    <button type="submit" class="btn btn-sm btn-danger">
                        Удалить
                    </button>
                </form> *@
            </td>
        </tr>
    }
</table>

@* <a asp-action="UserList">Список пользователей</a> *@